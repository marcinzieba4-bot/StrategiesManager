# Telegram Lambda Agent dependencies
# Pin minor versions; patch-level updates are safe to allow via pip install -r

anthropic>=0.40.0,<1.0
boto3>=1.34.0,<2.0
urllib3>=2.0.0,<3.0
python-dotenv>=1.0.0,<2.0   # only used locally via scripts/setup_webhook.py
